# Copy this file to .env and fill in your API keys

# Required: OpenAI API key (for GPT-4o vision + refinement)
OPENAI_API_KEY=sk-...

# Optional: Anthropic API key (for Claude as alternative)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Which LLM provider to use (openai or anthropic)
LLM_PROVIDER=openai

